<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Thème principal de l'application -->
    <style name="DiniM3akTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/orange</item>
        <item name="colorPrimaryVariant">@color/orange</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/black</item>
        <item name="android:fontFamily">@font/just_me_again_down_here</item>
        <item name="android:buttonStyle">@style/BaseButton</item>
    </style>

    <!-- Style de base pour tous les boutons -->
    <style name="BaseButton" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">12dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Bouton orange personnalisé -->
    <style name="OrangeButton" parent="BaseButton">
        <item name="android:backgroundTint">@color/orange</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Bouton Google -->
    <style name="GoogleButton" parent="BaseButton">
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:drawablePadding">8dp</item>
    </style>

    <!-- Bouton Apple -->
    <style name="AppleButton" parent="BaseButton">
        <item name="android:backgroundTint">@color/black</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:drawablePadding">8dp</item>
    </style>

</resources>
