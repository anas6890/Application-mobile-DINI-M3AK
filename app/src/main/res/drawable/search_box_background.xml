<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fond du champ -->
    <solid android:color="#FFFFFF" />

    <!-- Coins arrondis -->
    <corners android:radius="16dp" />

    <!-- Bordure (facultatif) -->
    <stroke
        android:width="1dp"
        android:color="#CCCCCC" />

    <!-- Marge intérieure (facultatif) -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>
